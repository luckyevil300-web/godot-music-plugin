<?xml version="1.0" encoding="utf-8"?>
<!--
    Definição do AppWidget.
    minWidth/minHeight: tamanho MÍNIMO (widget pequeno 2x1).
    maxResizeWidth/maxResizeHeight: tamanho MÁXIMO (widget grande 4x2).
    resizeMode: permite redimensionar horizontal e verticalmente.
    widgetCategory: suporte a tela inicial e tela de bloqueio (se disponível).
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:maxResizeWidth="500dp"
    android:maxResizeHeight="220dp"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_small"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_description"
    android:previewLayout="@layout/widget_medium" />
